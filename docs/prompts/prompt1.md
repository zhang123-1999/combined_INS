角色设定：你是一位资深的 C++ 导航算法工程师，擅长编写高内聚、低耦合的现代 C++ 代码（Modern C++）。任务目标：基于我提供的 ins_mech.cpp（系统模型）、ins_engine.cpp（当前引擎）和 eskf.cpp（当前 UWB 更新逻辑），请帮我重构代码，实现 "Mechanism (模型) - Engine (引擎) - Measurement (观测)" 的三层架构分离。具体重构要求：创建/重写 eskf_engine.h 和 .cpp (原 ins_engine)：将类名改为 EskfEngine。核心目标：使引擎通用化，移除所有特定传感器（如 UWB）的硬编码逻辑。删除 ApplyUwb 函数。新增一个通用的更新接口：void Correct(const Eigen::VectorXd &y, const Eigen::MatrixXd &H, const Eigen::MatrixXd &R);。在 Predict 函数中继续调用 InsMech 进行状态递推和协方差预测。新建 measurement_models.h (无 .cpp，头文件实现即可)：创建一个命名空间 MeasModels。封装 UWB 的观测模型计算逻辑。实现函数 ComputeUwbModel：输入预测状态、UWB 测量值、基站位置；输出残差 $y$、观测矩阵 $H$ 和 噪声矩阵 $R$。将原 eskf.cpp 中的 UwbUpdate 里的数学计算逻辑迁移到这里。更新主程序 eskf_fusion.cpp：在主循环中展示新的调用方式：调用 engine.Predict()。判断是否有 UWB 数据。调用 MeasModels::ComputeUwbModel 获取 $y, H, R$。调用 engine.Correct(y, H, R)。代码风格要求：使用 Eigen 库。保持 const 正确性（输入参数尽量用 const &）。代码清晰，添加关键注释。